steps:
  # Install dependency
  - name: "node:20"
    entrypoint: npm
    args: ["install"]

  # Build React
  - name: "node:20"
    entrypoint: npm
    args: ["run", "build"]

  # Deploy ke App Engine
  - name: "gcr.io/cloud-builders/gcloud"
    args: ["app", "deploy", "--quiet"]

timeout: "1600s"